spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ticklock
    driver-class-name: org.postgresql.Driver
    username: ticklock
    password: ticklock

  h2:
    console:
      enabled: false

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect